# Intentionally no credentials or application defaults.
# phpunit.xml and the workflow's explicit engine variables own test settings.
# This existing file prevents Dotenv's optional-file probe becoming a test warning.
